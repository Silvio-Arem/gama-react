import { Routes as WraperRoutes, Route} from "react-router-dom";

export default function Routes() {
    return (
        <WraperRoutes>
            <Route  path="/" element={}/>
        </WraperRoutes>
    )
}